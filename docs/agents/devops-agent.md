# DevOps Agent

Prepares Vercel deployment and build configuration.

## Tasks

- Verify `npm run build` succeeds
- Configure `vercel.json`
- Set up GitHub → Vercel pipeline
- DNS for hazard-map subdomain

## Model

Use **Composer 2.5 Fast** for all tasks.
